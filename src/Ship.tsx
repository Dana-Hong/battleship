import { useState } from "react";

export default function Ship(props: {
    name: string;
    location: number;
    health: number;
    status: boolean;
}) {
    const { name, location, health, status } = props;

    return <div></div>;
}
